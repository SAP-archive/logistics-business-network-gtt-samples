management:
  context-path: /system
  security:
    enabled: true
  health:
    defaults:
      enabled: false

security:
  basic:
    enabled: false

endpoints:
  health:
    enabled: false

server:
  port: 9098
  max-http-header-size: 65536

debug: false

com:
  sap:
    icd:
      mt:
        enabled: false

DESTINATION_GTT: {destination-name-of-gtt-flp-base-url}
HERE_MAP_KEY: {here-map-license-key}

VCAP_SERVICES: {vcap-services-env}