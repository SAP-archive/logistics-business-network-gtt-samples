management:
  context-path: /system
  security:
    enabled: true
  health:
    defaults:
      enabled: false

security:
  basic:
    enabled: false

endpoints:
  health:
    enabled: false

server:
  port: 9098
  max-http-header-size: 65536

debug: false

com:
  sap:
    icd:
      mt:
        enabled: false

GTT_CORE_ENGINE_TECHNICAL_USER: {techincal-user-name}
GTT_CORE_ENGINE_TECHNICAL_PWD: {techincal-user-pwd}
GTT_CORE_ENGINE_API_URL_FLP_BASED: https://{lbn-gtt-flp-base-url}/api

DESTINATION_GTT: gtt_core_engine_flp_base_url
HERE_MAP_KEY: {HERE_MAP_KEY}

VCAP_SERVICES: {destination_json}